const ErrorResponse = {
    success: false,
    error: 'Some server error'
}

const NotFoundResponse = {
    success: false,
    error: 'Some server error'
}

module.exports = {
    ErrorResponse,
    NotFoundResponse,
}